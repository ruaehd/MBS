<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Notice">
	<select id="selectNoticeList" resultType="com.mbs.mvc.vo.NoticeVO">
		SELECT * FROM `notice` ORDER BY ntc_no DESC
	</select>
	
	<select id="selectNoticeOne" parameterType="int" resultType="com.mbs.mvc.vo.NoticeVO">
	SELECT * FROM `notice` WHERE ntc_no=#{ntc_no}
	</select>
	
	<select id="selectNoticePrev" parameterType="int" resultType="int">
	SELECT ifnull(MAX(ntc_no), 0) FROM notice WHERE ntc_no <![CDATA[ < ]]> #{ntc_no}
	</select>

	<select id="selectNoticeNext" parameterType="int" resultType="int">
	SELECT ifnull(MIN(ntc_no), 0) FROM notice WHERE ntc_no <![CDATA[ > ]]> #{ntc_no}
	</select>
	
</mapper>